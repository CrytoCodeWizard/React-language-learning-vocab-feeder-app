const data = {};
const seenIds = []; 
const masteredIds = [];
const seenString = 'seen';
const masteredString = 'mastered';

module.exports = {
    data: data,
    seenIds: seenIds,
    masteredIds: masteredIds,
    seenString,
    masteredString
}